{
  "base_main_sha": "8224e6ca9800ab0ba1425d2a2bc3442bb5ba7e1a",
  "closure_commit": "09bd587f77f175be6193df9407f910478164e48e",
  "corrective_audits": [
    "#1 (audited head 38c6dc7): turn-wide semantic tool deduplication; a true turn-wide tool-call budget + distinct max_tool_calls_per_response; byte-exact Unicode tool-result bound. Corrective impl 938ffe3, re-close ae5b3da, record-green feaefaf.",
    "#2 (audited head feaefaf): agent.timeout_seconds enforced as the per-Agent total-turn deadline = min(agent, global); max_session_seconds enforced as an absolute session lifetime at admission, terminalising to a new EXPIRED state (migration c9e0f1a2b3c4) with agent.session.expired; new stable AgentTurnTimeoutError (NXS_AGENT_TURN_TIMEOUT, 504). Corrective impl 0b2aa3e, re-close 5e4393e, record-green 6550d12.",
    "#3 (audited head 6550d12): terminal-state absorption at the DATABASE transition boundary — _finish_turn re-checks the FOR UPDATE session state and discards a stale outcome rather than resurrecting EXPIRED -> ACTIVE; the whole-turn deadline is bounded by the session's remaining absolute lifetime so a turn opened just before expiry cannot outlive it. Corrective impl 19134ed, re-close 8a590a9, record-green 3ecc221.",
    "#4 (audited head 3ecc221): one idempotency key == one immutable logical turn == one model execution owner, decided at the DATABASE boundary — _open_turn returns (turn, session, owner); RUNNING/active -> AgentBusyError (never re-enters _run_turn); FAILED/CANCELLED -> new AgentIdempotentReplayError (NXS_AGENT_IDEMPOTENT_REPLAY, 409), no rerun; COMPLETED -> deterministic replay; an IntegrityError on the partial-unique-index insert resolves to AgentBusyError. Secondary: _stale_terminal_code maps the discarded-turn error_code truthfully (no more fabricated NXS_AGENT_SESSION_EXPIRED on a normal stop). Corrective impl 1b2ce1f, reopen+gate+evidence a8f5c6d, re-close 09bd587."
  ],
  "implementation_commit": "1b2ce1ffcfbc3a3ac24940e4f18d71a2be6cfb9e",
  "lineage": "Original close 05f5c8b, record-green 38c6dc7. Corrective #1: impl 938ffe3, re-close ae5b3da, record-green feaefaf. Corrective #2: impl 0b2aa3e, re-close 5e4393e, record-green 6550d12. Corrective #3: impl 19134ed, re-close 8a590a9, record-green 3ecc221. Corrective #4: impl 1b2ce1f, reopen+gate+evidence a8f5c6d, re-close 09bd587, and this record-green commit. NXS CI and NXS Security are both fully green (every job, push + pull_request) at the exact final head 09bd587.",
  "merged": false,
  "phase": "NXS-P13",
  "pull_request": 24,
  "record_green_ci_head": "09bd587f77f175be6193df9407f910478164e48e",
  "recorded_at": "2026-09-11T01:20:00Z",
  "result": "PASS",
  "runs": {
    "corrective3_final_3ecc221": {
      "jobs": ["state", "quality", "integration", "docker", "multiarch", "source-and-dependencies", "image"],
      "nxs_ci_pull_request": "success",
      "nxs_ci_push": "success",
      "nxs_security_pull_request": "success",
      "nxs_security_push": "success"
    },
    "corrective4_impl_1b2ce1f": {
      "jobs": ["state", "quality", "integration", "docker", "multiarch", "source-and-dependencies", "image"],
      "note": "carried on a8f5c6d / 09bd587 which contain 1b2ce1f.",
      "nxs_ci_pull_request": "success",
      "nxs_ci_push": "success",
      "nxs_security_pull_request": "success",
      "nxs_security_push": "success"
    },
    "corrective4_reclose_09bd587": {
      "jobs": ["state", "quality", "integration", "docker", "multiarch", "source-and-dependencies", "image"],
      "nxs_ci_pull_request": "success",
      "nxs_ci_push": "success",
      "nxs_security_pull_request": "success",
      "nxs_security_push": "success"
    }
  },
  "schema_version": "1.0.0"
}
