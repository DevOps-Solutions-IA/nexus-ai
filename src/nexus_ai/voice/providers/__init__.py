"""Voice provider adapters (NXS-P12).

``VoiceProviderAdapter`` is the stable, provider-neutral contract. ElevenLabs is one
implementation; a deterministic ``fake`` provider is a first-class one. The rest of Nexus
never imports an ElevenLabs type.
"""
