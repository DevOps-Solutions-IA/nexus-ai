# ADR-0004: Execution Guard

Status: Accepted. Every implementation begins only after the NXS guard validates branch, state, dependencies, mapping and lock. P00 implements the guard; future phases must consume it.
