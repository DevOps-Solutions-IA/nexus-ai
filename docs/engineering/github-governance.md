# GitHub Governance

The private repository is `DevOps-Solutions-IA/nexus-ai`; `main` is canonical. Required target protection is: pull requests, required NXS CI and NXS Security checks, resolved conversations, no force pushes, no deletion, and review for sensitive changes. The GitHub API returned HTTP 403 because private-repository branch protection requires a plan upgrade. Until available, maintainers must enforce the contract operationally and must not merge P00 without green checks.
