# Planned Component Boundaries

The backend app and shared packages will host HTTP/control-plane capabilities. Media gateway, workers, Sentinel and node agent are independently deployable planned services. Infrastructure reserves local, cell, Nomad, Asterisk, Kamailio and production boundaries. Except for `apps/backend` mapping to the minimal ASGI runtime, these are **PLANNED / NOT IMPLEMENTED** in P00.
